# 스마트 도서 관리 시스템 (Library Management System)

파이썬의 객체 지향 프로그래밍(OOP) 핵심 개념인 상속, 다형성, 그리고 메모리 최적화를 실습하기 위한 프로젝트입니다.
공통 속성을 가진 Item 클래스를 설계하고, 이를 확장하여 종이책(Book)과 전자책(EBook)을 효율적으로 관리합니다.

## 주요 기능
* **상속 및 다형성**: 부모 클래스를 기반으로 종이책, 전자책을 효율적으로 확장 구현했습니다.
* **메모리 최적화**: `__slots__`를 사용하여 객체 생성 시 발생하는 메모리 오버헤드를 최소화했습니다.
* **타입 검증**: `isinstance`를 활용하여 Library에 유효한 객체만 추가되도록 안정성을 높였습니다.
* **캡슐화**: 언더바(_)를 활용한 변수 설정으로 데이터 접근 권한을 관리합니다.

---

## 실행 방법 및 학습 포인트

```bash
# 1. 저장소 클론 (내 컴퓨터로 코드 복사)
git clone [https://github.com/sujih7-hwang/python-library-management.git](https://github.com/sujih7-hwang/python-library-management.git)

# 2. 프로젝트 폴더로 이동
cd python-library-management

# 3. 프로그램 실행
python library.py

# ---------------------------------------------------------
#  학습 포인트 (Learning Points)
# ---------------------------------------------------------
# - __slots__ 최적화: 파이썬 객체의 기본 __dict__ 생성을 억제하여 메모리 효율 극대화
# - 상속과 재사용성: Item 클래스를 기반으로 중복 코드를 제거하고 기능을 확장하는 설계
# - 다형성 활용: 동일한 메서드가 객체 타입에 따라 다르게 동작하는 오버라이딩 기법 습득
# - super() 계층 구조: 부모의 생성자와 메서드를 호출하여 계층적으로 기능을 확장하는 로직 구현
# - 방어적 프로그래밍: isinstance() 검증을 통해 유효한 데이터만 관리하여 시스템 안정성 확보
